<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/location_result_bg"
    android:paddingHorizontal="@dimen/spacing_normal"
    android:paddingVertical="@dimen/spacing_small">

    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_toStartOf="@id/clear"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/location_title_color"
        android:textSize="16sp"
        tools:text="Kings Landing" />

    <TextView
        android:id="@+id/distance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/name"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@id/clear"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/location_distance_color"
        android:textSize="16sp"
        tools:text="400m" />

    <ImageView
        android:id="@+id/clear"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/clear_location_desc"
        android:src="@drawable/ic_round_close_24"
        app:tint="@color/white" />

</RelativeLayout>